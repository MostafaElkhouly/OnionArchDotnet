using System;
using System.Collections.Generic;
using System.Text;

namespace Infrastructure.ViewModel.Enum
{
    public enum UserType
    {
        Parent = 1,
        Player = 2
    }
    public enum AdminUserType
    {
        Parent = 1,
        Admin = 2
    }
}
